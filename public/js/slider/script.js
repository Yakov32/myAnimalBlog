$(document).ready(function(){
	$('.slider').slick({
		arrows: false,
		fade: true,
		dots: true,
		adaptiveHeight:true,
		speed: 1000,
		autoplay:true,
		autoplaySpeed:2500, 
		touchThreshold:70,
		centerMode: true,

	});
});